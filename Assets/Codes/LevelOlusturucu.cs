using JetBrains.Annotations;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LevelOlusturucu : MonoBehaviour
{
    //public GameObject PatrickKlon;
    //public int PatrickNumber;
    //public float minX,maxX,minY,maxY;

    //void Start()
    //{
    //    Vector2 klonPosition = new Vector2();
    //    for (int i = 0; i < PatrickNumber; i++)
    //    {
    //        klonPosition.x = Random.Range(minX,maxX);
    //        klonPosition.y = Random.Range(minY,maxY);
    //        Instantiate(PatrickKlon,klonPosition,Quaternion.identity);
    //    }

    //}

    
}
